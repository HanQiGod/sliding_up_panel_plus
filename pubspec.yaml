name: sliding_up_panel_plus
description: Flutter 升级 3.35.7 之后，对 sliding_up_panel 插件的升级（After Flutter upgrade 3.35.7, the upgrade of sliding_up_panel plugins.）。
version: 2.1.0
homepage: https://github.com/HanQiGod/sliding_up_panel_plus

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
